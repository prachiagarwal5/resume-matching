{
    "name": "RESUME-MATCHING",
    "version": "1.0.0",
    "description": "Resume Matching",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "dev": "concurrently \"cd backend && npm i && npm run dev\" \"cd frontend && npm i && npm run dev\""
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "lucide-react": "^0.451.0",
        "mongoose": "^8.13.2",
        "react-icons": "^5.3.0",
        "react-to-print": "^3.0.1"
    },
    "devDependencies": {
        "concurrently": "^9.0.1"
    }
}
